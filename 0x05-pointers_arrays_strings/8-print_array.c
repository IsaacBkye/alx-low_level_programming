#include "main.h"
/**
 * print_array - Prints in reverse
 * @a: arg
 * @n: arg
 */
void print_array(int *a, int n)
{
	int d;
	int b;

	for (d = 0; d < n; d++)
	{
		_putchar(*(s + d * 4));
	}
	_putchar('\n');
}
