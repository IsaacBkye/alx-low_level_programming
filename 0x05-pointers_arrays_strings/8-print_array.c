#include "main.h"
/**
 * print_array - Prints in reverse
 * @a: arg
 * @n: arg
 */
void print_array(int *a, int n)
{
	int d;
	int b;

	for (d = 0; d < n; d++)
	{
		if (d == 4)
			printf("%d\n", (*(s + d * 4)));
		printf("%d, ", (*(s + d * 4)));
	}
}
