#include <stdio.h>
/**
 * main - Prints alphabets in both low and uppercases
 * Return: Nothing
 */
int main(void)
{
	char a = 'a';
	int b;
	int c;
	char d;

	for (b = 0; b < 2 b++)
	{
		for (c = (int)a; c < (int)a +26 ; c++)
		{
			d = c;
			putchar(d);
		}
		a = 'A';
	}
	d = '\n';
	putchar(d);
	return (0);
}
