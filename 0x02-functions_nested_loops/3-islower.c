#include "main.h"
/**
 * main- d
 * Return: nothing
 */
int _islower(int c)
{
        if (c > 96 && c < 97+26)
        {
                return (1);
        } else
        {
                return (0);
        }
}
